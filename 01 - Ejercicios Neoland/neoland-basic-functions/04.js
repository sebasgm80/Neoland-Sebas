const numbers = [12, 21, 38, 5, 45, 37, 6];

function average(arr) {
    let prom = arr[0];
    for (number of arr) {
            prom += number;
    }
    return prom / numbers.length;
}
let promedio = average(numbers);
console.log(promedio);